<div class="box">
    <h3>@ViewBag.personList.GridTitle</h3>

    <table class="table w-100">
        <thead>
            <tr>
                <th>Name</th>
                <th>Job Title</th>
                <th>Job Description</th>
            </tr>
        </thead>

        <tbody>
            @foreach (Person person in ViewBag.personList.Persons)
            {
                <tr>
                    <td>@person.Name</td>
                    <td>@person.JobTitle</td>
                    <td>@person.JobDescription</td>
                </tr>
            }
        </tbody>
    </table>
</div>