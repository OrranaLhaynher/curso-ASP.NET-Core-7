@model List<CityWeather>

<div class="margin-top-200 margin-left">
    <div class="flex">
        @foreach (CityWeather city in Model)
        {
            <vc:weather city="city"></vc:weather>
            //@await Component.InvokeAsync("Weather", new { city = city })
        }
    </div>
</div>